deb_packages:
  command: sudo apt install -y 
  packages:>
    python3 python3-pip python-dev
    python3-dev python-pip python3-neovim
    nodejs golang ruby ruby-dev
    curl tmux git vim zsh sqlite3 ctags  
    rdoc libsqlite3-dev
    openssh-client openssh-server 
    dconf-cli gnome-terminal
    postgresql pry rubygems fail2ban
    neovim asciinema docker mosh yarn
    silversearcher-ag virtualbox
    libssl1.0-dev libcurl4-openssl-dev 
    libxml2-dev
